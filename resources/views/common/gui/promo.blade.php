<a class="promo {{ $class ?? '' }}">
        
    <img src="{{ $img }}" alt="Фотопечать" class="promo__img">

    <p class="promo__title">{{ $title }}</p>
    
    <hr class="promo__line">

    <p class="promo__subtitle">{{ $text ?? '' }}</p>

</a>